export type User = {
    id: string;
    firstName: string;
    lastName: string;
    avatar: string;
}

/* export type Profile = {
    userId: string;
    firstName: string;
    secondName: string;
    avatar: string;
} */

export type Post = {
    id: string;
    title: string;
    content: string;
}